import { FilterByTipoPipe } from './filter-by-tipo.pipe';

describe('FilterByTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
